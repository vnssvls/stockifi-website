# Downloaded image binaries are regenerable from the CSVs via scripts/harvest-assets.py
# Keep them out of git; they will live in Cloudflare R2 post-migration.
assets/blog/
assets/customer-stories/
assets/testimonials/
assets/integrations/
assets/integration-categories/

# Keep the mapping (small, the source of truth for old-URL -> new-file)
!assets/asset-map.csv

# Competitor + current-site screenshots (regenerable) + node deps
assets/competitor-shots/
assets/current-site-shots/
scripts/node_modules/
scripts/package.json
scripts/package-lock.json

# Astro build (DC-474)
node_modules/
dist/
.astro/
.env
.env.production
.DS_Store
**/.DS_Store
test-results/
